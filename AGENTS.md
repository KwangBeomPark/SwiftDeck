# AGENTS.md

이 파일은 Codex/AI 에이전트 호환용 지침 파일입니다. 공통 개발 원칙의 정식 기준은 `myAGENT.md`이며, 프로젝트 구조와 기능 위치는 `AI_CODE_MAP.md`를 기준으로 확인합니다.

## 필수 확인 순서

1. 코드 작성, 수정, 삭제, 리팩토링 전에 `myAGENT.md`와 `AI_CODE_MAP.md`를 먼저 읽는다.
2. 기존 코드와 현재 파일 구조를 확인한 뒤, 요청 범위 안에서 가장 작은 안전한 변경을 적용한다.
3. 파일 구조, 함수명, 핵심 변수, 설정 경로, 단축키, 외부 의존성, 주요 기능 위치가 바뀌면 `AI_CODE_MAP.md`를 갱신한다.
4. 최종 답변은 한국어로 작성하고, 계획/변경 요약/자체 검토/변경 파일/문서 갱신/검증 결과를 간결히 포함한다.

## 핵심 작업 원칙

* 사용자와의 소통, 작업 계획, 변경 요약, 리스크 설명, 검증 결과는 한국어로 작성한다.
* 코드, 함수명, 변수명, 파일명, 명령어, 경로, 에러 메시지 원문은 필요한 경우 영어 또는 원문을 유지한다.
* 변수명, 함수명, 클래스명, 파일명, 설정 키, 기존 영문 주석, 기존 영문 UI 문구는 안정성과 공개 프로젝트 관례상 유지할 수 있으며, 스타일 목적만으로 한글화하지 않는다.
* 공개 문서에서는 개인 브랜딩을 유지할 수 있으나, 회사명, 내부 조직명, 실제 업무 경로, 직무상 민감한 표현은 중립화한다.
* 기존 단축키, 함수명, 변수명, 설정 키, 파일명을 임의로 변경하지 않는다.
* 사용자가 요청하지 않은 대규모 리팩토링이나 기능 추가를 하지 않는다.
* 비밀번호, API Key, Token, 개인 인증정보, 고도의 기밀정보를 코드, 문서, 샘플 설정에 넣지 않는다.
* 관리자 권한 없이 동작하는 Portable 방식을 우선한다.
* 회사 Windows 환경, 네트워크 드라이브, OneDrive/SharePoint 동기화 지연, 파일 잠김 가능성을 고려한다.

## AutoHotkey v2 규칙

* AutoHotkey v2 문법만 사용하고 v1 문법을 혼용하지 않는다.
* 함수 내부에서 외부 전역 변수를 수정하거나 접근할 때는 v2 규칙에 따라 `global` 선언을 명확히 한다.
* 단축키 정의, 메뉴 생성, 설정 로드, 액션 실행 로직을 가능한 분리한다.
* 신규 GUI 인스턴스 생성 전, 기존 동일 목적의 GUI 객체가 있으면 명시적으로 해제한다.
* GUI 작업 시 DPI scaling, 멀티 모니터, 화면 밖 표시 가능성을 검토한다.
* `AlwaysOnTop` 또는 플로팅 창 하위에서 대화상자가 가려질 수 있으면 `+OwnDialogs` 등 적절한 창 소유 관계를 검토한다.
* 클립보드, 파일, 레지스트리 읽기/쓰기에는 `try-catch`와 안전한 fallback을 적용한다.
* 파괴적 변경이나 덮어쓰기 작업에는 백업 또는 복구 가능성을 우선 고려한다.

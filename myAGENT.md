# myAGENT.md

이 문서는 이 프로젝트에서 AI 에이전트가 따라야 할 공통 개발 원칙입니다. 프로젝트 구조와 기능 위치는 `AI_CODE_MAP.md`를 기준으로 확인합니다.

## 1. 필수 워크플로우

AI 에이전트는 코드 작성, 수정, 삭제, 리팩토링 전에 반드시 아래 순서를 따른다.

1. `myAGENT.md`와 `AI_CODE_MAP.md`를 먼저 읽고 작업 맥락을 파악한다.
2. 현재 프로젝트의 폴더 구조, 주요 파일, 실행 흐름, 의존성을 확인한다.
3. 기존 코드를 먼저 읽고, 요청 범위 안에서 가장 작은 안전한 변경을 선택한다.
4. 코드 변경 후 파일 구조, 함수명, 핵심 변수, 설정 경로, 단축키, 외부 의존성, 주요 기능 위치가 바뀌면 `AI_CODE_MAP.md`를 갱신한다.
5. 최종 답변에는 계획, 변경 요약, 자체 검토, 변경 파일, 문서 갱신 여부, 검증 결과를 간결하게 포함한다.

## 2. 기본 소통 원칙

* 사용자와의 소통, 작업 계획, 변경 요약, 리스크 설명, 검증 결과는 기본적으로 한국어로 작성한다.
* 코드, 함수명, 변수명, 파일명, 명령어, 경로, 에러 메시지 원문은 필요한 경우 영어 또는 원문을 유지한다.
* 내부 사고 과정 전체를 노출하지 않고, 업무용 요약 리포트 형태로만 설명한다.

## 3. 공개 프로젝트 언어 및 식별자 원칙

* 변수명, 함수명, 클래스명, 파일명, 설정 키, 기존 영문 주석, 기존 영문 UI 문구는 안정성과 공개 프로젝트 관례상 유지할 수 있다.
* 스타일 목적만으로 식별자나 영문 주석을 한글로 바꾸지 않는다.
* 보안, 개인정보, 회사 정보 노출을 줄이기 위한 문구 정리는 허용한다.
* 공개 문서에서는 개인 브랜딩을 유지할 수 있으나, 회사명, 내부 조직명, 실제 업무 경로, 직무상 민감한 표현은 중립화한다.

## 4. 코딩 표준

* AutoHotkey v2 문법만 사용하고 v1 문법을 혼용하지 않는다.
* 기존 단축키, 함수명, 변수명, 설정 키, 파일명을 임의로 변경하지 않는다.
* 함수 내부에서 외부 전역 변수를 수정하거나 접근할 때는 v2 규칙에 따라 `global` 선언을 명확히 한다.
* 단축키 정의, 메뉴 생성, 설정 로드, 액션 실행 로직을 가능한 분리한다.
* 설정값, 경로, 매직 넘버는 가능한 상수 또는 설정 영역으로 분리한다.
* 오류를 조용히 무시하지 않고, 원인 파악이 가능한 메시지나 로그를 남긴다.

## 5. 금지 패턴

* 사용자가 요청하지 않은 대규모 구조 변경, 기능 추가, 단축키 변경을 하지 않는다.
* 사용자별 절대 경로, 회사 내부 경로, 개인 인증정보를 코드나 문서에 고정하지 않는다.
* 비밀번호, API Key, Token, 개인 인증정보, 고도의 기밀정보를 기본 설정이나 샘플 데이터에 넣지 않는다.
* 관리자 권한을 전제로 하는 동작을 기본값으로 만들지 않는다.
* 파괴적 삭제나 덮어쓰기는 앱이 소유한 임시 폴더 또는 명확히 지정된 대상에만 수행한다.

## 6. 파일 안전 규칙

* 사용자 파일과 업무 데이터를 보호한다.
* 파일 경로에 공백, 한글, 영어, 폴란드어 문자가 포함되어도 정상 동작해야 한다.
* 파일 잠김, 네트워크 드라이브 연결 실패, OneDrive/SharePoint 동기화 지연 가능성을 고려한다.
* 파일, 레지스트리, 클립보드, 외부 프로세스 접근에는 예외 처리를 적용한다.
* 공개 업로드 전에는 `.gitignore`, 백업 파일, 임시 테스트 파일, 빌드 산출물, 개인 경로 포함 여부를 확인한다.

## 7. UI 및 Windows 환경 기준

* 회사 Windows 환경에서 관리자 권한 없이 동작하는 Portable 방식을 우선한다.
* 무거운 외부 라이브러리나 DLL 의존성을 무분별하게 추가하지 않는다.
* 회사 보안 정책상 차단될 수 있는 방식은 피한다.
* 창 위치가 명시되지 않으면 커서 근처 또는 화면 중앙에 열리도록 계산한다.
* `AlwaysOnTop` 창이 있는 경우 신규 대화상자, InputBox, 메시지 박스가 가려지지 않도록 Z-Order와 `+OwnDialogs`를 검토한다.
* 멀티 모니터, DPI scaling, 화면 경계 밖 표시 가능성을 고려한다.

## 8. 빌드 산출물 및 릴리즈 파일 관리

* 소스 루트에는 사람이 관리하는 원본 소스, 문서, 공개 이미지 자산만 두는 것을 원칙으로 한다.
* 실행 파일, 압축 파일, 설치 파일, 패키징 결과물은 루트에 두지 않고 `dist/`, `release/`, `build/`, `out/` 같은 별도 폴더에 모아 관리한다.
* 빌드 산출물 폴더는 기본적으로 Git 저장소에 커밋하지 않고 `.gitignore`에 등록한다.
* 공개 배포용 `.exe`, `.zip`, installer 등은 저장소 본문이 아니라 GitHub Releases 같은 릴리즈 자산으로 업로드한다.
* 빌드 스크립트가 필요한 경우 `scripts/` 폴더에 `build.ps1`, `build.bat`, `build.sh` 등으로 분리하고, 산출물 출력 경로를 명확히 지정한다.
* README에는 소스 실행 방법과 릴리즈 다운로드 방법을 구분해 적고, 사용자가 루트 폴더의 산출물을 소스 파일로 오해하지 않도록 한다.
* 공개 업로드 전에는 산출물 폴더 안에 개인 설정, 로그, 회사 내부 경로, 사용자 데이터가 섞여 있지 않은지 별도로 확인한다.

## 9. 검증 루프

코드를 수정한 뒤에는 아래 항목을 점검하고 최종 답변에 요약한다.

* Plan: 요구사항, 수정 대상, 예상 리스크
* Draft Summary: 반영한 핵심 내용
* Self-Review: 문법, 실행, 경로, 권한, 인코딩, 기존 기능 영향
* Files Changed: 변경 파일
* Documentation Updated: 문서 갱신 여부
* Verification: 확인 사항과 남은 수동 테스트

## 10. 문서화 원칙

* 프로젝트별 구조, 함수 위치, 핵심 변수, 설정 경로, Registry key, 단축키, 임시 경로, 외부 의존성은 `AI_CODE_MAP.md`에 기록한다.
* `myAGENT.md`에는 프로젝트별 변경 이력을 넣지 않고 공통 개발 원칙만 유지한다.
* 공개 README는 실제 파일명 대소문자, 외부 통신 여부, 업로드 제외 대상이 독자에게 오해 없이 전달되도록 작성한다.

## 11. 테스트 원칙

* 변경 범위가 작더라도 최소한 관련 문법, 경로, 보안 검색을 수행한다.
* AutoHotkey v2 스크립트는 가능하면 `AutoHotkey64.exe /Validate`로 문법 검증한다.
* 기능 변경이 있으면 기존 단축키, 설정 파일 생성, GUI 표시, 클립보드/파일 I/O 영향 여부를 확인한다.
* 검증하지 못한 항목은 최종 답변에 남은 수동 테스트로 명시한다.
